{
  "mcpServers": {
    "resonant": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${CLAUDE_PROJECT_DIR:-.}/mcp-dist/server.mjs",
        "--root",
        "${CLAUDE_PROJECT_DIR:-.}"
      ]
    }
  }
}
